{
    "name": "paddel",
    "version": "1.0.0",
    "description": "Paddel League Platform",
    "scripts": {
        "build": "cd server && npm install && cd ../client && npm install && npm run build",
        "start": "cd server && node index.js"
    }
}